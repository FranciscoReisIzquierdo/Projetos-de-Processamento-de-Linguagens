"""
LEX
"""

literals = "+-/*=()"				## a single char

t_ignore = " \t\n"

tokens = [ 'VAR',   'NUMBER' ]

def t_VAR(t):
	r'[a-zA-Z_][a-zA-Z0-9_]*'
	t.value = t.value
	return t

def t_NUMBER(t):
	r'\d+(\.\d+)?'
	t.value = float(t.value)
	return t

def t_error(t):
	print(f"Illegal character '{tvalue[0]}', [{tlexerlineno}]", tlexerskip(1) )

"""
YACC
"""

precedence = [ ('left', '+', '-' ), ('left','*','/'), ('right','UMINUS') ]		# List of precedence

# symboltable : dictionary of variables

